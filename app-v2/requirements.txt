# Nano Sofa Studio v2 — pinned for reproducible Docker builds.
# Bump versions deliberately; this file is the source of truth for the image.
# Generated by `pip freeze` against the working .venv, then trimmed to direct
# deps (transitive deps are resolved by pip).

# --- Web framework ---
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.27

# --- Gemini SDK + image handling (shared with app/core/generator.py) ---
google-genai==2.0.0
Pillow==12.2.0
