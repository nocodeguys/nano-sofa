"""Nano Sofa Studio v2 backend package.

Imported as `studio.*` with app-v2/ on sys.path (the directory name contains a
dash, so the package cannot be addressed from the repo root). server.py puts
app-v2/ on sys.path before importing `studio.app`.
"""
